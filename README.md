# Vending Machine Patterns (OOP)

This project implements the class diagram patterns using Java and Maven.

## Requirements
- JDK that matches `pom.xml` (compiler source/target).
- Maven 3.x.

## Run
```powershell
mvn -q -DskipTests package
java -cp target/classes com.example.ute.Main
```

